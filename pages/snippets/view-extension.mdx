import Snippets from '../../components/snippets/Snippets'
import Anchor from '../../components/Anchor'
import Code from '../../components/Code'

export const meta = {
  title: 'View Extension',
  desc : 'A list of all View Extensions',
  author: 'Mohit Sharma'
}

export const index = [
  "Remove the View",
  "Show the View"
]

<Anchor> Remove the View </Anchor>

Change the visibilty of the `View` to `View.GONE`.

### Usage🎯
Call it on any `View`

<Code>{`myView.gone()
`}</Code>

### Code💻

<Code>{`fun View.gone() {
    this.visibility = View.GONE
}
`}</Code>

---

<Anchor>Show the View</Anchor>

Change the visibilty of the `View` to `View.VISIBLE`.

### Usage🎯
Call it on any `View`

<Code>{`myView.visible()
`}</Code>

### Code💻

<Code>{`fun View.visible() {
    this.visibility = View.VISIBLE
}
`}</Code>

---

<Anchor>Resize the View</Anchor>

Set Width and height of a View Programmatically.

### Usage🎯
Call it on any `View`
<Code>{`
myView.resize(50 , 100)
`}</Code>

### Code💻

<Code>{`
fun View.resize(width: Int, height: Int) {
    val lp = this.layoutParams
    lp?.let {
        lp.width = width
        lp.height = height
        layoutParams = lp
    }
}
`}</Code>












export default ({ children }) => <Snippets meta ={meta}>{children}</Snippets>